{
  "name": "bitchat-web-preview",
  "private": true,
  "version": "1.0.0",
  "description": "Interactive browser preview of the native bitchat interface.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && node server.mjs",
    "preview": "npm run start",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@noble/ciphers": "2.1.1",
    "@noble/curves": "2.0.1",
    "@noble/hashes": "2.0.1",
    "@zxing/browser": "0.1.5",
    "@zxing/library": "0.21.3",
    "fflate": "0.8.2",
    "nostr-tools": "2.24.1",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "esbuild": "0.25.8"
  }
}
